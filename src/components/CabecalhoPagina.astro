---
/** Cabeçalho das páginas de apoio: trilha opcional, título e descrição. */
interface Props {
  titulo: string;
  descricao?: string;
  trilha?: { nome: string; href: string };
}

const { titulo, descricao, trilha } = Astro.props;
---

<header class="cabecalho-pagina">
  {trilha && <a class="trilha" href={trilha.href}>{trilha.nome}</a>}
  <h1>{titulo}</h1>
  {descricao && <p class="descricao">{descricao}</p>}
  <slot />
</header>

<style>
  .cabecalho-pagina {
    padding: 48px 0 28px;
  }

  .trilha {
    font-size: 15px;
    color: var(--ink-2);
  }

  h1 {
    margin: 8px 0 0;
    font-weight: 800;
    font-size: clamp(34px, 5vw, 52px);
    line-height: 1.05;
    letter-spacing: -0.012em;
  }

  .descricao {
    max-width: 60ch;
    margin: 12px 0 0;
    font-size: 18px;
    color: var(--ink-2);
  }
</style>
